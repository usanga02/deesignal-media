import React from "react";

type Props = {
  label: string;
  extraClass?: string;
};

const MiddleNav = ({ label, extraClass }: Props) => {
  return (
    <p>Hello</p>
  )
};

export default MiddleNav;
